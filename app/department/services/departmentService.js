(function() {
    'use strict';

    angular
        .module('spa')
        .service('departmentService', departmentService);

    /* @ngInject */
    function departmentService(dependencies) {
        this.func = func;

        ////////////////

        function func() {
        }
    }
})();